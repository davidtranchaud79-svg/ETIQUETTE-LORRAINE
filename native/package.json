{
  "name": "etiquette-lorraine-native",
  "private": true,
  "version": "5.1.0",
  "type": "module",
  "scripts": {
    "prepare": "node prepare-native.mjs",
    "android:add": "npm run prepare && npx cap add android",
    "ios:add": "npm run prepare && npx cap add ios",
    "sync": "npm run prepare && npx cap sync",
    "android:open": "npx cap open android",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@capacitor/ios": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "typescript": "^5.9.2"
  }
}
